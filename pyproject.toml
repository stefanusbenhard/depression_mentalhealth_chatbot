[project]
name = "depression-chatbot"
version = "0.1.0"
description = "Depression mental health chatbot"
requires-python = ">=3.11,<3.12"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
